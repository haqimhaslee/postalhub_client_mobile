import 'package:flutter/material.dart';

class ParcelAll extends StatelessWidget {
  const ParcelAll({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
        body: Center(
      child: Card(
        child: SizedBox(
          height: 50,
          width: 1000,
        ),
      ),
    ));
  }
}
